# DRISHTI runtime dependencies.
#
# Deliberately short. numpy does the modelling, FastAPI serves it, and that is
# the whole hard requirement. No GDAL, no rasterio, no GPU stack - the target is
# a district server or a field laptop where a heavyweight geospatial install is
# not realistic.
fastapi>=0.110
uvicorn[standard]>=0.27
numpy>=1.24

# Optional. Install only if you have real GeoTIFF elevation or flood masks to
# feed in; every code path that uses it degrades cleanly when it is absent.
# rasterio>=1.3

# Development only.
# pytest>=7.4
# httpx>=0.26
