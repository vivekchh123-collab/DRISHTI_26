# DRISHTI runtime dependencies.
# Deliberately lightweight: FastAPI serves the API, NumPy handles data processing, 
# and Pydantic manages data validation without heavy geospatial stacks.

fastapi>=0.110
uvicorn[standard]>=0.27
numpy>=1.24
pydantic>=2.0

# Optional: Uncomment if raster processing is required locally
# rasterio>=1.3
